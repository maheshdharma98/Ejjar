import MaterialCommunityIcons from '@react-native-vector-icons/material-design-icons';

// Drop-in re-export — all consumers use: import Icon from './Icon' (or relative path)
// This gives real vector icons. All <Icon name="xxx" size={n} color="#..." /> calls work unchanged.
export default MaterialCommunityIcons;
